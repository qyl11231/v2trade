# V2-Trade 系统架构校准与致命缺口清单

> **文档目标**：校准当前系统架构文档中的不一致性，并明确致命缺口清单，确保后续研发不踩坑。

**文档版本**：v1.0  
**最后更新**：2026-01-06  
**校准依据**：代码实际实现 + 架构设计原则

---

## 📋 目录

1. [架构校准（解决不一致）](#一架构校准解决不一致)
2. [致命缺口清单](#二致命缺口清单)
3. [事件契约定义](#三事件契约定义)
4. [时间语义规范](#四时间语义规范)
5. [指标计算冲突策略](#五指标计算冲突策略)
6. [指标订阅与聚合绑定规则](#六指标订阅与聚合绑定规则)

---

## 一、架构校准（解决不一致）

### 1.1 指标模块进度校准

**问题**：冻结版显示指标模块阶段3/4/5未完成，新人指南显示已完成

**实际代码检查结果**：
- ✅ `IndicatorCalculator`已完整实现（监听BarClosedEvent、查询订阅、计算、持久化）
- ✅ `IndicatorValueRepository`已实现幂等写入（insert ignore）
- ✅ `CalcFingerprint`已实现冲突检测
- ✅ `IndicatorEngineRouter`已实现引擎路由
- ✅ `BarSeriesManager`已实现Bar系列管理

**校准结论**：
- **指标模块核心功能已闭环**（阶段0.5、1、2、3、4、5已完成）
- **阶段6（可观测性）部分完成**：Metrics已实现，日志已结构化
- **阶段7（API查询）已完成**：`IndicatorController`提供只读API

**更新建议**：
- 冻结版文档需要更新阶段3/4/5状态为✅已完成
- 新人指南的描述是正确的，但需要补充说明"阶段6部分完成"

---

### 1.2 行情订阅粒度校准

**问题**：1m订阅 vs 多周期订阅的混淆

**实际实现情况**：
1. **WebSocket订阅**：当前只订阅1分钟K线（`candle1m`频道）
2. **K线聚合**：从1m聚合生成5m、15m、30m、1h、4h
3. **指标计算**：只响应5m、15m、30m、1h、4h的`BarClosedEvent`（不处理1m）

**校准结论**：
```
订阅链路：OKX WebSocket → 只订阅1m → QuestDB kline_1m
聚合链路：kline_1m → 聚合引擎 → QuestDB kline_5m/15m/30m/1h/4h → 发布BarClosedEvent
指标链路：BarClosedEvent (5m/15m/30m/1h/4h) → IndicatorCalculator → 计算指标
```

**BarClosedEvent发布策略（已冻结）**：
- ✅ **仅聚合周期发布**：5m、15m、30m、1h、4h
- ❌ **1m不发布**：指标模块不处理1m周期（代码第84-90行明确过滤）
- ✅ **发布时机**：聚合完成后立即发布

**更新建议**：
- 新人指南明确说明：订阅只支持1m，多周期通过聚合生成
- 架构文档明确：BarClosedEvent只由聚合周期触发

---

### 1.3 时间语义校准

**问题**：QuestDB ts是开盘时间还是收盘时间不明确

**实际代码检查结果**：
- `BarClosedEvent.barCloseTime`：明确注释为"bar_close_time，UTC"
- `NormalizedBar.barTime`：明确注释为"bar_close_time，UTC"
- 代码中使用了`QuestDbTsSemanticsProbe`探针检测QuestDB时间语义

**校准结论**（基于代码推断和架构设计）：

```
QuestDB表（kline_1m/kline_5m等）：
- ts字段：bar_open_time（开盘时间，UTC）
- 语义：K线周期的起始时间点

指标模块内部（NormalizedBar/BarClosedEvent）：
- barTime/barCloseTime：bar_close_time（收盘时间，UTC）
- 语义：K线周期的结束时间点

转换规则：
- bar_close_time = bar_open_time + 周期时长
- 例如：1m周期，bar_open_time = 2026-01-06 10:00:00，则bar_close_time = 2026-01-06 10:01:00
```

**更新建议**：
- 新人指南明确说明：QuestDB ts = bar_open_time
- 补充时间语义转换文档（见第4节）

---

### 1.4 密码安全校准

**问题**：MD5不适合密码存储

**当前状态**：
- SQL脚本使用MD5：`0192023a7bbd73250516f069df18b500`（admin123）
- 生产环境存在安全隐患

**校准结论**：
- ❌ **必须改为BCrypt**：使用Spring Security的`BCryptPasswordEncoder`
- ⚠️ **迁移计划**：
  1. 修改`UserService`使用BCrypt加密
  2. 提供数据迁移脚本（将现有MD5密码重置或迁移）
  3. 更新登录逻辑

**更新建议**：
- 新人指南标注"MD5密码存储需要改为BCrypt"
- 创建技术债务清单，标记为P0

---

### 1.5 策略域边界校准

**问题**：signal_intent vs strategy_intent_record的关系不明确

**表设计分析**：

```
signal_intent（信号意图表）：
- 作用：将Signal转换为Intent（LATEST_ONLY模型）
- 状态：ACTIVE/CONSUMED/EXPIRED/IGNORED
- 时机：Signal接收后，策略消费前

strategy_intent_record（策略决策流水表）：
- 作用：记录策略的最终决策（"我决定做什么"）
- 动作：OPEN/CLOSE/ADD/REDUCE/REVERSE/HOLD
- 时机：策略运行时决策后

关系：
Signal → signal_intent → StrategyRuntime → strategy_intent_record
```

**校准结论**：
- ✅ **设计正确**：信号意图和策略决策是两层，分离是合理的
- ⚠️ **实现状态**：两个表都已创建，但策略运行时未实现
- ✅ **开发顺序**：应先实现Signal → signal_intent，再实现StrategyRuntime

**更新建议**：
- 明确说明：signal_intent是"阶段2"要实现的（信号消费层）
- strategy_intent_record是"阶段2策略运行时"要实现的（决策记录层）

---

## 二、致命缺口清单

### 2.1 P0（必须闭环，否则系统只是拼图）

#### 缺口1：BarClosedEvent的完整语义与发布策略

**当前状态**：
- ✅ 事件类已定义（`BarClosedEvent.java`）
- ✅ 字段已明确（tradingPairId, symbol, timeframe, barCloseTime等）
- ⚠️ **缺少**：幂等键定义、事件序列号、发布策略文档

**必须补齐**：
1. **幂等键定义**：如何唯一标识一个BarClosedEvent？
   - 建议：`(tradingPairId, timeframe, barCloseTime)` 三元组
2. **事件序列号**：是否需要sequence字段防止乱序？
   - 建议：可选，但建议添加用于调试
3. **发布策略文档**：明确哪些周期发布、发布时机、重复发布处理

**影响**：如果不明确，指标计算可能出现重复计算或遗漏

---

#### 缺口2：指标计算的幂等落库与冲突检测策略

**当前状态**：
- ✅ `indicator_value`表有唯一索引
- ✅ `CalcFingerprint`已实现
- ✅ `IndicatorCalculator`已实现冲突检测逻辑

**必须补齐**：
1. **冲突检测策略文档**：详细说明什么情况下算CONFLICT
2. **冲突处理规则**：是否覆盖？是否记录日志？是否告警？
3. **数据质量标记规则**：何时标记PARTIAL/INVALID？

**当前代码逻辑**（基于IndicatorCalculator.java）：
```java
// 写入使用insert ignore
WriteResult result = valueRepository.insertIgnore(value);

// 如果写入失败（冲突），检测指纹
if (result == WriteResult.IGNORED) {
    IndicatorValue existing = valueRepository.findOneKey(...);
    if (!fingerprint.equals(existing.getCalcFingerprint())) {
        // 记录CONFLICT日志
        calcLog.setCalcStatus(FAILED);
        calcLog.setErrorMsg("CONFLICT: fingerprint mismatch");
    }
}
```

**必须文档化**：这个逻辑需要写入文档，明确冲突策略

---

#### 缺口3：BarSeriesManager的内存生命周期规则

**当前状态**：
- ✅ `BarSeriesManager`已实现
- ⚠️ **缺少**：内存上限、淘汰策略、缺Bar处理规则

**必须补齐**：
1. **冷启动加载规则**：
   - 当前代码：加载最近365根（`QuestDbKlineReader`）
   - 是否可配置？是否需要根据指标min_required_bars动态调整？
2. **内存上限规则**：
   - 每个pair+timeframe保留多少根Bar？
   - 是否实现LRU淘汰？
3. **缺Bar处理规则**：
   - 如果QuestDB缺少某根Bar，`data_quality`如何标记？
   - 是否触发校准任务？

**影响**：如果不明确，可能出现内存溢出或指标计算错误

---

### 2.2 P1（系统可观测、可运维）

#### 缺口4：指标计算Metrics与日志结构化

**当前状态**：
- ✅ `IndicatorMetrics`已定义（cost_ms, fail_count, conflict_count）
- ✅ 结构化日志已使用MDC
- ⚠️ **缺少**：Metrics导出配置文档、日志字段清单

**必须补齐**：
1. **Metrics导出**：如何查看？Prometheus端点？Grafana面板？
2. **日志字段清单**：MDC中包含哪些字段？格式规范？

---

#### 缺口5：校准任务与指标数据质量联动

**当前状态**：
- ✅ 校准任务已实现（缺失检测、数据核对）
- ❌ **缺少**：校准发现缺口后，如何联动指标模块？

**必须补齐**：
1. **联动规则**：校准发现缺失K线 → 补全后 → 是否触发指标重算？
2. **质量标记**：校准发现的异常数据，如何标记到`indicator_value.data_quality`？
3. **补算任务**：是否需要独立的"指标补算任务"？

---

### 2.3 P2（策略阶段2：决策生成）

#### 缺口6：StrategyRuntime的消费规则

**当前状态**：
- ❌ StrategyRuntime未实现

**必须明确**：
1. **消费模式**：如何消费Signal和Indicator？
   - 轮询？事件驱动？
2. **数据源**：
   - Signal：从`signal`表查询？还是订阅SignalReceivedEvent？
   - Indicator：调用`IndicatorController` API？还是直接查表？
3. **决策时机**：何时触发决策？定时？事件触发？

---

## 三、事件契约定义

### 3.1 BarClosedEvent契约

**事件类**：`com.qyl.v2trade.indicator.domain.event.BarClosedEvent`

**字段列表**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `tradingPairId` | Long | ✅ | 交易对ID（系统内部引用） |
| `symbol` | String | ✅ | 交易对符号（如：BTC-USDT-SWAP） |
| `timeframe` | String | ✅ | K线周期（5m/15m/30m/1h/4h，不支持1m） |
| `barCloseTime` | LocalDateTime | ✅ | K线收盘时间（bar_close_time，UTC） |
| `open` | BigDecimal | ✅ | 开盘价 |
| `high` | BigDecimal | ✅ | 最高价 |
| `low` | BigDecimal | ✅ | 最低价 |
| `close` | BigDecimal | ✅ | 收盘价 |
| `volume` | BigDecimal | ✅ | 成交量 |
| `sourceCount` | int | ✅ | 聚合使用的源K线数量（5m=5，15m=15等） |
| `eventTime` | LocalDateTime | ✅ | 事件产生时间（本地时间，用于调试） |

**幂等键定义**：
```
唯一标识：(tradingPairId, timeframe, barCloseTime)
```

**发布策略**：
1. **发布时机**：K线聚合完成后立即发布
2. **发布周期**：仅5m、15m、30m、1h、4h（不支持1m）
3. **重复发布**：理论上不应重复，但幂等键保证不会重复计算
4. **发布顺序**：按时间顺序发布（barCloseTime递增）

**事件序列**（可选，建议添加）：
- 当前未实现sequence字段
- 建议：添加`long sequence`字段，用于调试和乱序检测

---

### 3.2 KlineEvent契约

**事件类**：`com.qyl.v2trade.market.model.event.KlineEvent`

**字段列表**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `symbol` | String | ✅ | 交易对符号 |
| `interval` | String | ✅ | 周期（当前只支持1m） |
| `timestamp` | Long | ✅ | K线时间戳（UTC毫秒） |
| `open` | BigDecimal | ✅ | 开盘价 |
| `high` | BigDecimal | ✅ | 最高价 |
| `low` | BigDecimal | ✅ | 最低价 |
| `close` | BigDecimal | ✅ | 收盘价 |
| `volume` | BigDecimal | ✅ | 成交量 |

**幂等键定义**：
```
唯一标识：(symbol, interval, timestamp)
```

**发布策略**：
1. **发布时机**：WebSocket接收到K线数据后立即发布
2. **发布周期**：当前只支持1m
3. **消费者**：MarketDataCenter（写入QuestDB）

---

## 四、时间语义规范

### 4.1 时间基准

**系统唯一时间基准**：UTC（协调世界时）

- 所有数据库时间字段使用UTC
- 所有事件时间戳使用UTC
- 用户输入的时间字符串直接当作UTC处理（不进行时区转换）

---

### 4.2 QuestDB时间语义

**表结构**：`kline_1m`, `kline_5m`, `kline_15m`, `kline_30m`, `kline_1h`, `kline_4h`

**ts字段语义**：
```
ts = bar_open_time（K线开盘时间，UTC）
```

**示例**：
```
1分钟K线：
- ts = 2026-01-06 10:00:00（UTC）→ 表示10:00:00-10:01:00这根K线

5分钟K线：
- ts = 2026-01-06 10:00:00（UTC）→ 表示10:00:00-10:05:00这根K线

1小时K线：
- ts = 2026-01-06 10:00:00（UTC）→ 表示10:00:00-11:00:00这根K线
```

---

### 4.3 MySQL时间语义

**indicator_value.bar_time字段语义**：
```
bar_time = bar_close_time（K线收盘时间，UTC）
```

**转换公式**：

```
对于1分钟K线：
bar_close_time = bar_open_time + 1分钟

对于5分钟K线：
bar_close_time = bar_open_time + 5分钟

对于15分钟K线：
bar_close_time = bar_open_time + 15分钟

对于30分钟K线：
bar_close_time = bar_open_time + 30分钟

对于1小时K线：
bar_close_time = bar_open_time + 1小时

对于4小时K线：
bar_close_time = bar_open_time + 4小时
```

**Java代码实现**（示例）：
```java
// 从QuestDB ts（开盘时间）转换为收盘时间
public static LocalDateTime toBarCloseTime(LocalDateTime barOpenTime, String timeframe) {
    int minutes = parseTimeframeToMinutes(timeframe);
    return barOpenTime.plusMinutes(minutes);
}

// 解析周期为分钟数
private static int parseTimeframeToMinutes(String timeframe) {
    return switch (timeframe) {
        case "1m" -> 1;
        case "5m" -> 5;
        case "15m" -> 15;
        case "30m" -> 30;
        case "1h" -> 60;
        case "4h" -> 240;
        default -> throw new IllegalArgumentException("Unsupported timeframe: " + timeframe);
    };
}
```

---

### 4.4 时间对齐规则

**K线时间必须对齐到周期起始点**：

```
1分钟K线：对齐到分钟（秒、毫秒为0）
- 正确：2026-01-06 10:00:00
- 错误：2026-01-06 10:00:15

5分钟K线：对齐到5分钟倍数（分钟%5=0，秒、毫秒为0）
- 正确：2026-01-06 10:00:00, 10:05:00, 10:10:00
- 错误：2026-01-06 10:03:00

1小时K线：对齐到小时（分钟、秒、毫秒为0）
- 正确：2026-01-06 10:00:00, 11:00:00
- 错误：2026-01-06 10:30:00
```

**代码实现**（PeriodCalculator.alignTimestamp）：
```java
public static LocalDateTime alignTimestamp(LocalDateTime timestamp, String timeframe) {
    int minutes = parseTimeframeToMinutes(timeframe);
    LocalDateTime aligned = timestamp
        .withSecond(0)
        .withNano(0);
    
    if (minutes >= 60) {
        // 小时级别：对齐到小时
        aligned = aligned.withMinute(0);
    } else {
        // 分钟级别：对齐到周期倍数
        int minute = aligned.getMinute();
        int alignedMinute = (minute / minutes) * minutes;
        aligned = aligned.withMinute(alignedMinute);
    }
    
    return aligned;
}
```

---

### 4.5 时间语义转换示例

**场景**：从QuestDB读取1小时K线，用于指标计算

```
1. QuestDB查询结果：
   symbol: BTC-USDT
   ts: 2026-01-06 10:00:00（UTC，开盘时间）
   open: 50000.0
   high: 51000.0
   low: 49000.0
   close: 50500.0
   volume: 100.5

2. 转换为NormalizedBar：
   tradingPairId: 1
   symbol: BTC-USDT
   timeframe: "1h"
   barTime: 2026-01-06 11:00:00（UTC，收盘时间 = 开盘时间 + 1小时）
   open: 50000.0
   high: 51000.0
   low: 49000.0
   close: 50500.0
   volume: 100.5

3. 发布BarClosedEvent：
   barCloseTime: 2026-01-06 11:00:00（UTC）
   （其他字段同上）

4. 写入indicator_value：
   bar_time: 2026-01-06 11:00:00（UTC，收盘时间）
   （指标值...）
```

---

## 五、指标计算冲突策略

### 5.1 冲突场景

**场景1：同一唯一键，不同指纹（CONFLICT）**
```
条件：
- 唯一键相同：(userId, tradingPairId, timeframe, barTime, indicatorCode, indicatorVersion)
- 计算指纹不同：fingerprint != existingValue.calcFingerprint

示例：
- 第一次计算：RSI(period=14)，fingerprint=abc123
- 第二次计算：RSI(period=20)，fingerprint=def456
- 结果：CONFLICT（参数不一致）
```

**场景2：同一唯一键，相同指纹（SUCCESS，幂等）**
```
条件：
- 唯一键相同
- 计算指纹相同：fingerprint == existingValue.calcFingerprint

示例：
- 第一次计算：RSI(period=14)，fingerprint=abc123，写入成功
- 第二次计算：RSI(period=14)，fingerprint=abc123，insert ignore忽略
- 结果：SUCCESS（幂等，不重复写入）
```

---

### 5.2 计算指纹生成规则

**指纹算法**：SHA-256

**输入字符串格式**：
```
{indicatorCode}:{indicatorVersion}:{sortedParams}:{engine}
```

**参数排序规则**：
- 按key的字典序排序
- 格式：`key1=value1,key2=value2,...`

**示例**：
```java
// 输入
indicatorCode = "RSI"
indicatorVersion = "v1"
params = {"period": 14, "smooth": 1}
engine = "ta4j"

// 构建字符串（参数按key排序）
"RSI:v1:period=14,smooth=1:ta4j"

// SHA-256哈希
fingerprint = "a1b2c3d4e5f6..."（64位十六进制）
```

**代码实现**：见`CalcFingerprint.java`

---

### 5.3 冲突处理流程

**完整流程**（基于IndicatorCalculator.java）：

```
1. 计算指标
   ↓
2. 生成计算指纹
   fingerprint = CalcFingerprint.generate(code, version, params, engine)
   ↓
3. 写入指标值（幂等）
   WriteResult result = valueRepository.insertIgnore(value)
   ↓
4. 判断结果
   ├─ INSERTED → 写入成功，记录SUCCESS日志
   └─ IGNORED → 写入被忽略（唯一键冲突），进入冲突检测
       ↓
5. 冲突检测
   existingValue = valueRepository.findOneKey(...)
   if (fingerprint != existingValue.calcFingerprint) {
       // CONFLICT：参数不一致
       calcLog.setCalcStatus(FAILED)
       calcLog.setErrorMsg("CONFLICT: fingerprint mismatch. " +
                          "Expected: " + fingerprint + ", " +
                          "Actual: " + existingValue.calcFingerprint)
   } else {
       // 幂等：相同参数重复计算
       calcLog.setCalcStatus(SUCCESS)
       calcLog.setErrorMsg("IDEMPOTENT: duplicate calculation with same fingerprint")
   }
   ↓
6. 记录日志（无论成功或冲突，都记录）
   calcLogRepository.insert(calcLog)
```

---

### 5.4 冲突处理规则

**规则1：CONFLICT不覆盖原值**
- 如果检测到CONFLICT，**不覆盖**已存在的指标值
- 原因：保证数据一致性，避免错误数据覆盖正确数据
- 处理：记录FAILED(CONFLICT)日志，告警通知（待实现）

**规则2：幂等写入不计为错误**
- 如果指纹相同，即使写入被忽略，仍记录SUCCESS日志
- 原因：这是正常的幂等行为，不是错误

**规则3：冲突日志必须完整**
- `indicator_calc_log`表必须记录：
  - `calc_status`: FAILED
  - `error_msg`: 包含CONFLICT详情和指纹对比
  - `calc_fingerprint`: 本次计算的指纹

---

### 5.5 数据质量标记规则

**indicator_value.data_quality字段**：

| 值 | 含义 | 触发条件 |
|---|------|---------|
| `OK` | 正常 | 计算成功，Bar数据完整 |
| `PARTIAL` | 部分数据 | Bar数据不完整（缺少部分历史Bar） |
| `INVALID` | 无效 | 计算失败或数据异常 |

**标记规则**（待实现）：
1. **OK**：默认值，计算成功
2. **PARTIAL**：
   - BarSeries中的Bar数量 < min_required_bars
   - 但计算仍能进行（使用已有数据）
3. **INVALID**：
   - 计算失败（引擎抛出异常）
   - 检测到CONFLICT

---

## 六、指标订阅与聚合绑定规则

### 6.1 订阅流程

**用户操作**：
```
用户通过API或UI创建指标订阅：
- userId: 1
- tradingPairId: 1
- timeframe: "1h"
- indicatorCode: "RSI"
- indicatorVersion: "v1"
- params: {"period": 14}
```

**系统处理**：
1. 插入`indicator_subscription`表
2. 检查聚合配置（`aggregation_config`）：是否有该pair+timeframe的聚合任务
3. 如果没有，**自动创建聚合配置**（待实现）
4. 系统启动时，`BarSeriesManager`加载该订阅的历史数据

---

### 6.2 聚合与指标计算的绑定

**核心规则**：**指标计算依赖聚合生成的Bar**

**流程**：
```
1. 用户订阅指标（如：1h周期的RSI）
   ↓
2. 系统检查：是否有1h聚合配置？
   ├─ 有 → 继续
   └─ 没有 → 自动创建（待实现）
   ↓
3. 聚合引擎从1m聚合生成1h K线
   ↓
4. 聚合完成后，发布BarClosedEvent(timeframe="1h")
   ↓
5. IndicatorCalculator接收事件
   ↓
6. 查询订阅：找到所有订阅了该pair+1h的指标
   ↓
7. 对每个订阅计算指标
```

**关键点**：
- ✅ **指标不直接依赖1m**：指标模块不订阅1m的BarClosedEvent
- ✅ **指标依赖聚合周期**：只有聚合生成的周期（5m/15m/30m/1h/4h）才触发指标计算
- ⚠️ **自动创建聚合配置**：当前需要手动创建，建议自动创建

---

### 6.3 聚合配置规则

**聚合配置表**（`aggregation_config`，当前可能未实现，需要确认）：

| 字段 | 说明 |
|------|------|
| `trading_pair_id` | 交易对ID |
| `target_timeframe` | 目标周期（5m/15m/30m/1h/4h） |
| `source_timeframe` | 源周期（固定为1m） |
| `enabled` | 是否启用 |

**自动创建规则**（待实现）：
```
当用户订阅指标时：
1. 检查是否存在聚合配置（tradingPairId + targetTimeframe）
2. 如果不存在，自动创建：
   - source_timeframe = "1m"
   - target_timeframe = 订阅的timeframe
   - enabled = true
3. 触发聚合引擎启动（如果需要）
```

---

### 6.4 实时计算 vs 聚合触发

**问题**：订阅了1h指标，是靠聚合生成1h bar触发，还是实时从1m拼？

**答案**：**靠聚合表 + bar closed 触发**

**原因**：
1. **数据一致性**：聚合表保证数据完整性（对齐、去重）
2. **性能**：聚合表查询比实时拼接快
3. **事件驱动**：BarClosedEvent保证计算的实时性

**流程对比**：

❌ **错误做法（实时拼接）**：
```
1m更新 → 实时拼接1h → 触发计算
问题：可能使用未完成的1h bar，数据不准确
```

✅ **正确做法（聚合触发）**：
```
1m更新 → 聚合引擎等待1h完成 → 写入聚合表 → 发布BarClosedEvent → 触发计算
优势：保证1h bar完整性，数据准确
```

---

### 6.5 缺Bar处理规则

**场景**：如果QuestDB缺少某根1m K线，导致聚合的1h bar不完整

**处理规则**（待完善）：

1. **聚合时检测**：
   - 聚合引擎检测1h周期内的1m bar是否完整
   - 如果缺失，标记`source_kline_count < 60`（1h应该有60根1m）

2. **指标计算时检测**：
   - IndicatorCalculator检查BarSeries的完整性
   - 如果缺少历史Bar，标记`data_quality = PARTIAL`

3. **校准任务联动**：
   - 校准任务发现缺失K线 → 补全 → 触发聚合重算 → 触发指标重算（待实现）

---

## 七、总结

### 7.1 校准结果

| 问题 | 校准结论 | 状态 |
|------|---------|------|
| 指标模块进度 | 核心功能已闭环，阶段6部分完成 | ✅ 已校准 |
| 行情订阅粒度 | 订阅1m，聚合生成多周期，指标只响应聚合周期 | ✅ 已校准 |
| 时间语义 | QuestDB ts=开盘时间，指标barTime=收盘时间 | ✅ 已校准 |
| 密码安全 | 必须改为BCrypt | ⚠️ 待实现 |
| 策略域边界 | signal_intent和strategy_intent_record分离正确 | ✅ 已校准 |

---

### 7.2 致命缺口优先级

**P0（必须立即补齐）**：
1. ✅ 事件契约定义（第3节）
2. ✅ 时间语义规范（第4节）
3. ✅ 指标计算冲突策略（第5节）
4. ✅ 指标订阅与聚合绑定规则（第6节）

**P1（近期补齐）**：
5. ⏳ BarSeriesManager内存生命周期规则
6. ⏳ 校准任务与指标数据质量联动
7. ⏳ Metrics和日志文档

**P2（后续补齐）**：
8. ⏳ StrategyRuntime消费规则
9. ⏳ 密码BCrypt迁移

---

### 7.3 后续行动

1. **更新新人指南**：根据校准结果更新`docs/项目总结-新人入门指南.md`
2. **更新冻结版文档**：更新指标模块进度状态
3. **实现技术债务**：密码BCrypt迁移
4. **补齐P0缺口**：完善BarSeriesManager规则、校准任务联动

---

**文档维护者**：系统架构组  
**审核日期**：2026-01-06

